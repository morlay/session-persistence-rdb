lib/
node_modules/
*.tgz

# vendored deepseek-harness（monorepo 成员，git clone 或复制，不入库）
vendor/deepseek-harness/
.tmp/

# 依赖解析由 pnpm-workspace.yaml 驱动（正常版本 + workspace 匹配），
# lock 文件不入库。
nub.lock
pnpm-lock.yaml
package-lock.json
npm-shrinkwrap.json
